# Runtime data directory

This directory is intentionally empty in Git. A NAS deployment receives only
the redacted `shadow-beta-snapshot.json` and `shadow-ledger.jsonl` files here;
the dashboard may create `control-state.json`. Do not place credentials,
cookies, browser profiles, full logs, or private screenshots in this directory.
